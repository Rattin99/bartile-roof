'use client';

import TileConfigurator from "@/views/TileConfigurator";

// This is the main page, effectively replacing the route for "/"
export default function Home() {
  return <TileConfigurator />;
}
